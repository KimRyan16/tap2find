{
  "name": "tap2find-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "yarn --cwd backend start",
    "dev": "yarn --cwd backend xian"
  }
}
